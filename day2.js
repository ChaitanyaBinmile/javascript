// Map and Set

// creating map
let hel = new Map();

// storing values using set keyword
hel.set('1', 'first_value');
hel.set("2", "second_value");

// some features that can be used with map but not with objects
console.log(hel.get("1"));
console.log(hel.size);
